FOR USE:

	Simply download and run the python script, which will prompt user to select which news source to retrieve today's news headlines from. Implemented utilizing requests, Beautiful Soup, and lxml libraries. 
	
PURPOSE:

	This project was coded to implement web-scraping libraries and approaches, which could be further scaled to provide expanded information scraping (such as headline blurbs, associated images, links to the full articles, etc).
